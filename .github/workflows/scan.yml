name: scan instances every hour
on:
  schedule:
    - cron: '0 * * * *'
jobs:
  build:
    runs-on: self-hosted
    steps:
    - name: scan instances
      run: |
        cd ${{ env.DIRECTORY }}
        python3 scan.py
